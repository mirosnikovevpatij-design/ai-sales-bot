Если удобнее через CMD: открой cmd, вставь по очереди (каждую строку — Enter).
Первый раз может спросить логин/пароль GitHub (или токен вместо пароля).

--- Залить на GitHub ---
cd /d "C:\Users\eweti\OneDrive\Рабочий стол\AsistentBot"
git add .
git commit -m "Update"
git push origin main

--- Скачать с GitHub (к себе на компьютер) ---
cd /d "C:\Users\eweti\OneDrive\Рабочий стол\AsistentBot"
git pull origin main

--- Обновить сервер (выполнять ПОСЛЕ того как залил на GitHub) ---
Вариант А: двойной клик по 3_обновить_сервер.bat (в начале файла укажи USER и HOST сервера).
Вариант Б: зайти на сервер по SSH и вставить в терминал:
  cd /var/www/ai-sales-bot
  ./deploy.sh
